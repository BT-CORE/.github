---
title: "Identité"
description: "Contrats de disponibilité, d'identifiants et d'identité civile."
icon: "code"
---

Appeler ces exports serveur avec `Open77.exports.call(...):await()`. Sauf indication contraire, le joueur doit posséder un personnage chargé.

## `isReady`

Indique si le personnage du joueur est chargé en mémoire.

```lua
--- isReady(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return boolean ready `true` si le personnage est chargé ; sinon `false`.

local ready = Open77.exports.call("bt_core", "isReady", playerId):await()
```

## `getUserId`

Retourne l'identifiant Open2077 persistant du joueur.

```lua
--- getUserId(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string? userId Identifiant Open2077 persistant, ou `nil` s'il est indisponible.

local userId = Open77.exports.call("bt_core", "getUserId", playerId):await()
```

## `getCharacterId`

Retourne l'identifiant persistant du personnage actif.

```lua
--- getCharacterId(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string? characterId Identifiant persistant du personnage, ou `nil` s'il est indisponible.

local characterId = Open77.exports.call("bt_core", "getCharacterId", playerId):await()
```

## `getFirstname`

Retourne le prénom du personnage.

```lua
--- getFirstname(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string firstname Prénom, ou une chaîne vide si le personnage est indisponible.

local firstname = Open77.exports.call("bt_core", "getFirstname", playerId):await()
```

## `setFirstname`

Modifie le prénom du personnage chargé.

**Paramètres :** `value` est une chaîne non vide de 32 caractères maximum.

```lua
--- setFirstname(playerId, value)
---@param playerId integer Identifiant runtime du joueur. Le personnage doit être chargé pour cette modification.
---@param value string Prénom non vide, limité à 32 caractères.
---@return boolean success `true` si la valeur est acceptée ; `false` si le personnage est indisponible ou la valeur invalide.

local ok = Open77.exports.call("bt_core", "setFirstname", playerId, "V"):await()
```

## `getLastname`

Retourne le nom du personnage.

```lua
--- getLastname(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string lastname Nom, ou une chaîne vide si le personnage est indisponible.

local lastname = Open77.exports.call("bt_core", "getLastname", playerId):await()
```

## `setLastname`

Modifie le nom du personnage chargé.

**Paramètres :** `value` est une chaîne non vide de 32 caractères maximum.

```lua
--- setLastname(playerId, value)
---@param playerId integer Identifiant runtime du joueur. Le personnage doit être chargé pour cette modification.
---@param value string Nom non vide, limité à 32 caractères.
---@return boolean success `true` si la valeur est acceptée ; `false` si le personnage est indisponible ou la valeur invalide.

local ok = Open77.exports.call("bt_core", "setLastname", playerId, "Mercer"):await()
```

## `getFullName`

Retourne le prénom et le nom assemblés.

```lua
--- getFullName(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string fullName Nom complet nettoyé, ou une chaîne vide s'il est indisponible.

local fullName = Open77.exports.call("bt_core", "getFullName", playerId):await()
```

## `getDateOfBirth`

Retourne la date de naissance au format ISO `YYYY-MM-DD`.

```lua
--- getDateOfBirth(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string? dateOfBirth Date ISO enregistrée, ou `nil` si elle est indisponible.

local dateOfBirth = Open77.exports.call("bt_core", "getDateOfBirth", playerId):await()
```

## `setDateOfBirth`

Modifie la date de naissance au format ISO `YYYY-MM-DD`.

**Paramètres :** `isoDate` doit respecter `YYYY-MM-DD`. Aucune valeur par
défaut n'est appliquée.

```lua
--- setDateOfBirth(playerId, isoDate)
---@param playerId integer Identifiant runtime du joueur. Le personnage doit être chargé pour cette modification.
---@param isoDate string Date respectant `YYYY-MM-DD` ; aucune valeur par défaut.
---@return boolean success `true` si la valeur est acceptée ; `false` si le personnage est indisponible ou la valeur invalide.

local ok = Open77.exports.call("bt_core", "setDateOfBirth", playerId, "2045-06-12"):await()
```

## `getSex`

Retourne la valeur de sexe du personnage.

```lua
--- getSex(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string sex Valeur enregistrée, ou une chaîne vide si elle est indisponible.

local sex = Open77.exports.call("bt_core", "getSex", playerId):await()
```

## `setSex`

Modifie la valeur de sexe du personnage chargé.

**Paramètres :** `value` accepte toute chaîne de 16 caractères maximum.
L'onboarding fournit actuellement `male` ou `female`. Aucune valeur par défaut
n'est appliquée.

```lua
--- setSex(playerId, value)
---@param playerId integer Identifiant runtime du joueur. Le personnage doit être chargé pour cette modification.
---@param value string Chaîne de 16 caractères maximum ; l'onboarding utilise `male` ou `female` ; aucun défaut.
---@return boolean success `true` si la valeur est acceptée ; `false` si le personnage est indisponible ou la valeur invalide.

local ok = Open77.exports.call("bt_core", "setSex", playerId, "female"):await()
```

## `getLifepath`

Retourne l'origine Cyberpunk du personnage.

```lua
--- getLifepath(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string lifepath Valeur canonique enregistrée, ou une chaîne vide si elle est indisponible.

local lifepath = Open77.exports.call("bt_core", "getLifepath", playerId):await()
```

## `setLifepath`

Modifie l'origine avec `corpo`, `streetkid` ou `nomad`.

**Paramètres :** `value` doit être `corpo`, `streetkid` ou `nomad`. Aucune
valeur par défaut n'est appliquée.

```lua
--- setLifepath(playerId, value)
---@param playerId integer Identifiant runtime du joueur. Le personnage doit être chargé pour cette modification.
---@param value 'corpo'|'streetkid'|'nomad' Origine canonique ; aucune valeur par défaut.
---@return boolean success `true` si la valeur est acceptée ; `false` si le personnage est indisponible ou la valeur invalide.

local ok = Open77.exports.call("bt_core", "setLifepath", playerId, "nomad"):await()
```

## `getPhoneNumber`

Retourne le numéro unique du personnage.

```lua
--- getPhoneNumber(playerId)
---@param playerId integer Identifiant runtime du joueur. Un personnage non chargé reçoit la valeur de repli documentée.
---@return string? phoneNumber Numéro unique enregistré, ou `nil` s'il est indisponible.

local phoneNumber = Open77.exports.call("bt_core", "getPhoneNumber", playerId):await()
```
